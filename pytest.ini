[pytest]
testpaths = tests
# conftest.py aporta los ayudantes (crear_usuario, entrar, subir) que los
# tests importan por nombre.
pythonpath = . tests
filterwarnings =
    error::DeprecationWarning:app
